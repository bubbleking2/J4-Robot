/*-------------------------------------------------------------------------------------------------*/
//File #2
/*-------------------------------------------------------------------------------------------------*/

#define GLOBAL_VARIABLES 1

//Motor Ports
#define LEFT_MOTOR 1
#define RIGHT_MOTOR 3

//Digital Ports
//#define LEFT_ENCODER 2
//#define RIGHT_ENCODER 3
#define LEFT_OPTOSENSOR 4
#define CENTER_OPTOSENSOR 5
#define RIGHT_OPTOSENSOR 6
#define LOWER_CDS_PORT 16
#define UPPER_CDS_PORT 17

//Servo Ports
//#define GPS 4

//Speeds
#define FOLLOW_SPEED 35

//Point Locations
struct Location{
    int x;
    int y;
}
start = {0, 24},
traffic = {0, 190},
trafficLeft = {-15, 215},//{-10, 193},
buttonStart = {-47, 190},
button = {-47, 180},
topCenterRamp = {0,165},
bottomCenterRamp = {0, 80},
crankLineStart = {-45, 167},
crankLineEnd = {872, 167};

/*-------------------------------------------------------------------------------------------------*/
//Function #1
/*-------------------------------------------------------------------------------------------------*/

void SetLocations(int courseDirection)
{
    #ifdef TEST_LOG
      {
        LogEvent(211);
    }
    #endif
    
    start.x *= courseDirection;
    traffic.x *= courseDirection;
    buttonStart.x *= courseDirection;
    button.x *= courseDirection;
    topCenterRamp.x *= courseDirection;
    bottomCenterRamp.x *= courseDirection;
    crankLineStart.x *= courseDirection;
    crankLineEnd.x *= courseDirection;
    
    #ifdef TEST_LOG
      {
        LogEvent(210);
    }
    #endif
}

/*-------------------------------------------------------------------------------------------------*/
/*-------------------------------------------------------------------------------------------------*/
