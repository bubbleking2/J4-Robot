/*-------------------------------------------------------------------------------------------------*/
//File #8
/*-------------------------------------------------------------------------------------------------*/

#ifdef DRIVE

#define FROM_TO 1

/*-------------------------------------------------------------------------------------------------*/
//Function #1
/*-------------------------------------------------------------------------------------------------*/

void FromStartToTraffic(int courseDirection)
{
    #ifdef TEST_LOG
      {
        LogEvent(811);
    }
    #endif
    
    GetTo(topCenterRamp.x, topCenterRamp.y, 100.);
    
    #ifdef TEST_LOG
      {
        LogEvent(810);
    }
    #endif
}

/*-------------------------------------------------------------------------------------------------*/
//Function #2
/*-------------------------------------------------------------------------------------------------*/

void FromTrafficToButtonStart(int courseDirection)
{
    int process;
    
    #ifdef TEST_LOG
      {
        LogEvent(821);
    }
    #endif
    
    TurnDegrees(-90 * courseDirection);
    msleep(500L);
    GetTo(buttonStart.x, buttonStart.y, 100.);   
    
    #ifdef TEST_LOG
      {
        LogEvent(820);
    }
    #endif
}

/*-------------------------------------------------------------------------------------------------*/
//Function #3
/*-------------------------------------------------------------------------------------------------*/

void FromButtonToCrank(int courseDirection)
{
    #ifdef TEST_LOG
      {
        LogEvent(831);
    }
    #endif
    
    GetTo(topCenterRamp.x, topCenterRamp.y, 100.);
    facingLeft = 0;
    GetTo(crankLineStart.x, crankLineStart.y, 100.);
    
    #ifdef TEST_LOG
      {
        LogEvent(830);
    }
    #endif
}

/*-------------------------------------------------------------------------------------------------*/

#endif
