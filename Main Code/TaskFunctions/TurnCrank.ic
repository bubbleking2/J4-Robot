/*-------------------------------------------------------------------------------------------------*/
//File #8
/*-------------------------------------------------------------------------------------------------*/

#if DRIVE * GPS == 1

#define HANG_LIGHT 1

/*-------------------------------------------------------------------------------------------------*/
//Function #3
/*-------------------------------------------------------------------------------------------------*/

void TurnCrank(int courseDirection)
{
    int processID;
    
    #ifdef TEST_LOG
      {
        LogEvent(831);
    }
    #endif
    
    TurnAgainst(crankLine.x, crankLine.y);
    
    RunWhileFor(DriveDistance(24., -40.), analog(CENTER_OPTOSENSOR) > FOLLOW_THRESHOLD && analog(LEFT_OPTOSENSOR) > FOLLOW_THRESHOLD && analog(RIGHT_OPTOSENSOR) > FOLLOW_THRESHOLD, 2000L);
    
    #ifdef TEST_LOG
      {
        LogEvent(612);
    }
    #endif
    
    RunFor(FollowLineBackwards(), 2000L);
    
    #ifdef TEST_LOG
      {
        LogEvent(682);
    }
    #endif
    
    motor(CRANK, 100);
    msleep(1250L);
    motor(CRANK, 0);
    
    #ifdef TEST_LOG
      {
        LogEvent(831);
    }
    #endif
}

/*-------------------------------------------------------------------------------------------------*/
/*-------------------------------------------------------------------------------------------------*/

#endif
