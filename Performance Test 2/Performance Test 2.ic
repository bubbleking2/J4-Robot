#define LEFT_MOTOR 1
#define RIGHT_MOTOR 4

int main ()
{
    while (!start_button ());
    motor(LEFT_MOTOR,100);
    motor(RIGHT_MOTOR,-100);
    printf ("motors running");
    while(!stop_button());
    motor (LEFT_MOTOR,0);
    motor (RIGHT_MOTOR, 0);
    printf("\ndone");
}
