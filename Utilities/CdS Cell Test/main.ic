//CdS Cell Test

#define LOWER_CDS_PORT 4

void main()
{
    printf("\nPress Start to begin.");
    start_press();
    
    while(!stop_button())
      {
        printf("\nReading: %d", analog(LOWER_CDS_PORT));
        msleep(200L);
    }
    
    printf("\nOperation Terminated");
    
}
