#use StringToFunction.ic

void RunStringArray(int numberOfInstructions, char instructions[][])
{
    int i = 0;
    while(i < numberOfInstructions)
      {
        StringToFunction(instructions[i]);
        i++;
    }
}
