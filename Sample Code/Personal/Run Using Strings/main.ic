#use RunStringArray.ic

#define NUMBER_OF_INSTRUCTIONS 2

void main()
{
    char instructions[NUMBER_OF_INSTRUCTIONS][] = {
        "Initialize Course",
          "Initialize GPS"
    };
    
    RunStringArray(NUMBER_OF_INSTRUCTIONS, instructions);
}
