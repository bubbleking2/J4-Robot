#use CourseInitializeKnob.ic
#use GPSModule.ic

int course;

void StringToFunction(char string[])
{
    if(string[0] == "Initialize"[0])
      {
        if(string[11] == "Course"[0])
          {
            course = CourseInitializeKnob();
        }
        else if(string[11] == "GPS"[0])
          {
            GPSInitialize(course);
        }
    }
}
